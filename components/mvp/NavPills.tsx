'use client';

/* Navigation is handled conversationally through Callum.
   This component is intentionally empty — the user talks to Callum to navigate. */
export default function NavPills() {
  return null;
}
